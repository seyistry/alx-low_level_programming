#include <stdio.h>
#include "main.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	/**
	 * main - Entry point
	 *
	 * Return: void (Success)
	 */
	char i = 'a';

	while (i < '{')
	{
		_putchar(i);
		i++;
	}
	_putchar('\n');
}

