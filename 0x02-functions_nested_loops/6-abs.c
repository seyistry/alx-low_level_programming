#include <stdio.h>
#include "main.h"

/**
 * _abs - check if value C is lowercase
 *@n: n is interger
 *
 *Return: return an int n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = -1 * n
		return (n);
	}
	else
	{
		return (n);
	}
}
